name: timestamp_camera
description: "A new Flutter project."

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0
  geolocator: ^13.0.0
  geocoding: ^3.0.0
  intl: ^0.19.0
  permission_handler: ^11.3.0
  path_provider: ^2.1.2
  image: ^4.1.7
  gal: ^2.3.0

flutter:
  uses-material-design: true
